import React from 'react';

// Todo: Make navbar docked on side instead of at the top
// Extra credit, allow the user to determine where it should be placed...

/**
 * Website navigation bar.
 */
function Navbar() {
  return(
    <h1> hello there</h1>
  );
}

export {Navbar};